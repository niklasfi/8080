8080 is an open source otr mirror.

to run it install node.js from http://nodejs.org (I'm currently working with v0.3.7 (the apis tend to change quite quickly)) and apply the desired options in your settings2.json. Finally type `node server2.js`
